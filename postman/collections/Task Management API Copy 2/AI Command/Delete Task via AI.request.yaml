$kind: http-request
url: "{{base_url}}/ai/command"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "prompt": "Hapus task ID 2."
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
