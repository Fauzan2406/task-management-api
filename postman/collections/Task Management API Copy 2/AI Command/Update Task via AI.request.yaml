$kind: http-request
url: "{{base_url}}/ai/command"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "prompt": "Ubah status task ID 5 jadi 'done'."
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
